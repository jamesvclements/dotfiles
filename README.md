```bash
git clone https://github.com/jamesvclements/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
git submodule update --init --recursive  # Fetch private configs
./setup.sh
```

See [AGENTS.md](./AGENTS.md) for full documentation.
